# .-.aliases.python can be used to add any Ruby specific commands

if (exists python); then
    test "$DOTSILENT" == false && echo -e "*\tPython\t\t\t\t*"
    alias python_serve='python -m SimpleHTTPServer'
fi