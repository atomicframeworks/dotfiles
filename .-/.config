# config.cfg
# Used to set up configuration variables for .- dotfiles

# Possible config variables
# SILENT        : Hide what .- dotfiles are loading (no echo)
# AUTOUPDATE    : Git pull the .- dotfile directory at load
# SUDOALWAYS    : Ask for sudo at start and keep sudo alive
SILENT=true
AUTOUPDATE=false
SUDOALWAYS=false
